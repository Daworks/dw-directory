{
  "name": "dw-directory",
  "version": "2.0.0",
  "description": "WordPress Directory Service Plugin",
  "private": true,
  "scripts": {
    "build": "gulp sass",
    "watch": "gulp sass:watch"
  },
  "devDependencies": {
    "gulp": "^5.0.0",
    "gulp-sass": "^6.0.0",
    "sass": "^1.83.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
